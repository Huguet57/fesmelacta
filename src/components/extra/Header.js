function Header() {
    return (
        <header>
            <h1>Transcripció automàtica en català</h1>
            <p id="warning-desktop" role="alert">Aquesta aplicació requereix Google Chrome i anar per ordinador per funcionar correctament.</p>
        </header>
    );
}

export default Header;