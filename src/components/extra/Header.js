function Header() {
    return (
        <header>
            <h1>Transcripció automàtica d'àudios (llargs!)</h1>
            <p id="warning-desktop">Aquesta aplicació requereix Google Chrome i anar per ordinador per funcionar correctament.</p>
        </header>
    );
}

export default Header;